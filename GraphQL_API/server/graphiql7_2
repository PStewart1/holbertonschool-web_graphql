mutation {
    addTask(
        title: "Reboot Syling", 
        weight: 2, 
        description: "Add Bootstrap to reset the CSS styling"
        ) {
      title
      weight
      description
    }
}
